[pytest]
asyncio_mode = auto
testpaths = tests
python_files = test_*.py
addopts = -ra --strict-markers --cov=app --cov-report=term-missing --cov-fail-under=90
markers =
    unit: fast, no external dependencies
    integration: requires a running database/redis
    api: exercises FastAPI routes via TestClient
